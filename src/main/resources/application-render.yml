spring:
  jpa:
    properties:
      hibernate:
        format_sql: true
        use_sql_comments: true # has overhead, not for production
    hibernate:
      ddl-auto: validate

  datasource:
    username: catan
    password:
    url: jdbc:postgresql://dpg-cs0l1ljtq21c73ef8e4g-a.frankfurt-postgres.render.com/catandb_g03a

  jackson:
    default-property-inclusion: NON_NULL

logging:
  level:
    org:
      hibernate:
        SQL: debug
        orm:
          jdbc:
            bind: trace
springdoc:
  api-docs:
    path: "/api-docs"